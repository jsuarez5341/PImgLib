#PImgLib

INSTALLATION:

1. Move the PImgLib folder to the desired installation directory.
2. Open a terminal. cd into PImgLib (do NOT run install from outside the folder)
3. Execute: bash install
4. If you ever need to change the install directory, you will have to repeat these steps.
